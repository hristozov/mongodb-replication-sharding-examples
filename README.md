# mongodb-replication-sharding-examples
MongoDB replication/sharding demos.

Uses Docker Compose.

## Demo 1 - simple replica set
3 instances are spawned - one "master" and two "slaves".

## Demo 2 - sharding
3 instances are spawned
